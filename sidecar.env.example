# Copy to:  $HERMES_HOME/zhc-fabric/sidecar.env
#   (default: ~/.hermes/zhc-fabric/sidecar.env)
# Then:     chmod 600 ~/.hermes/zhc-fabric/sidecar.env
# Start:    scripts/install-sidecar.sh start
#
# Optional alternative: put the same ZHC_FABRIC_DEFAULT_* lines in
#   ~/.hermes/.env
# (path from: hermes config env-path)

# Required — OpenAI-compatible base ending in /v1
ZHC_FABRIC_DEFAULT_BASE_URL=http://127.0.0.1:11434/v1
DEFAULT_BASE_URL=http://127.0.0.1:11434/v1

# Required — model id your server expects
ZHC_FABRIC_DEFAULT_MODEL=llama3.2
DEFAULT_MODEL=llama3.2

# Optional — leave empty for most local servers; set for cloud
ZHC_FABRIC_DEFAULT_API_KEY=
DEFAULT_API_KEY=

# Optional
MAX_INFLIGHT_COMPLETIONS=2
