# Python stub (fallback only)

Stdlib HTTP server used for offline contract tests and
`FABRIC_RUNTIME=python ./scripts/install-sidecar.sh start`.

**Not the product path.** Users should run the Erlang/OTP image via Docker
(`../docker-compose.yml` → `../otp/`).
